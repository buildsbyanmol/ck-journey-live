# Category tile icons

Downloaded 25 Aug 2026 from `https://asset21.ckassets.com/resources/image/category/<msite_asset>`,
where `msite_asset` is the per-tile filename `data/categories.json` recorded at harvest. 16 files,
byte-for-byte as served, one GET each — same precedent as the 309 store logos (content-gaps.md §7.8).
The URL base was read off the live homepage HTML, not guessed. Served at `/tiles/<file>` via
`publicDir`.
